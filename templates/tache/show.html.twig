{% extends 'base.html.twig' %}

{% block title %}Taches{% endblock %}

{% block content %}
    <h1>Taches</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tache.id_tache }}</td>
            </tr>
            <tr>
                <th>NomTache</th>
                <td>{{ tache.nom }}</td>
            </tr>
            
            <tr>
                <th>Date</th>
                <td>{{ tache.date ? tache.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tache_index') }}">back to list</a>

    <a href="{{ path('app_tache_edit', {'id': tache.id_tache}) }}">edit</a>

    {{ include('tache/_delete_form.html.twig') }}
{% endblock %}
