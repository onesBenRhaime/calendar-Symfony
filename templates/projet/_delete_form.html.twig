<form method="post" action="{{ path('app_projet_delete', {'id': projet.id_projet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projet.id_projet) }}">
    <button class="btn">Delete</button>
</form>
